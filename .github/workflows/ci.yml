name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.x"
          cache: true

      - name: Verify Go version
        run: go version

      - name: Download dependencies
        run: go mod download

      - name: Install tools
        run: |
          go install mvdan.cc/gofumpt@latest
          go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest

      - name: Check go mod tidy
        run: |
          go mod tidy
          git diff --exit-code

      - name: Format check (gofumpt)
        run: |
          gofumpt -w -l .
          git diff --exit-code

      - name: Lint (golangci-lint)
        run: golangci-lint run ./...

      - name: Run tests
        run: go test -v ./...

  # -----------------------------
  # Future Frontend Job (Disabled for now)
  # Uncomment when Next.js added
  # -----------------------------
  # frontend:
  #   name: Frontend Checks
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: apps/web
  #
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Node
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 20
  #         cache: "npm"
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Lint
  #       run: npm run lint
  #
  #     - name: Build
  #       run: npm run build